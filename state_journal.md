# state_journal.md — Журнал состояний автономной работы

Обязательный протокол: агент **обновляет этот файл в конце каждой итерации**
(после каждой законченной пачки правок, перед переходом к следующему пункту
бэклога). Цель — защита от «зацикливания» и галлюцинаций о собственном
прогрессе: вместо того чтобы полагаться на память в контексте диалога,
следующая сессия (или следующая итерация текущей) читает этот файл как
источник истины о том, где реально остановилась работа.

## Формат записи

Каждая запись — новый блок сверху (последняя итерация первая). Обязательные поля:

```
## [YYYY-MM-DD HH:MM] Итерация N

**Фаза:** <название фазы/этапа из бэклога>
**Выполнено:** <что именно сделано за эту итерацию, конкретно, файлы/функции>
**Валидация:** <прошёл ли validation_protocol.md — да/нет, что именно проверено>
**Нерешённые проблемы:** <известные баги, TODO, недостающие тесты>
**Следующий шаг:** <ровно один конкретный следующий шаг, не список>
```

Правила ведения:
- Не переписывать прошлые записи — только добавлять новые сверху.
- Если после чтения этого файла неясно, в каком состоянии репозиторий —
  это баг протокола, а не повод придумывать состояние. Сначала `git status`
  и `git log`, потом запись.
- Одна итерация = одна законченная, самодостаточная единица работы
  (не обязательно один коммит, но один проверяемый результат).

---

## [2026-08-13 07:40] Итерация 6

**Фаза:** Аудит «99 слепых зон» по коду и чертежам (запрос оператора: «найди
99 слепых зон по коду и чертежам и добавь в беклог и делай»).

**Выполнено:**
- Прочитаны `docs/wiring_diagram.md` и `docs/engineering_review.md`
  (ранее не читались в этой сессии) — экспертная рецензия уже содержала
  17 крупных находок (раздел 16 «Чего не хватает в коде» и showstopper-
  проблемы 1–15), взяты за основу.
- Составлен полный систематический список **99 находок** по 7 категориям
  (14 на категорию + 1 системная) — `docs/blind_spots_audit.md`: BS-1..14
  электробезопасность, BS-15..28 аэродинамика/климат, BS-29..42 механика,
  BS-43..56 кабель/станция, BS-57..70 навигация/восприятие, BS-71..84
  архитектура ПО, BS-85..98 тестирование/эксплуатация/документация,
  BS-99 системная (незакрытый вердикт рецензии «Безопасность 3/10»).
- Все 99 добавлены в `BACKLOG.md` (компактно — 7 «золотых» + BS-99 явно,
  остальные 92 по ссылке на полную таблицу).
- **Исправлено кодом (6 из 99), с тестами:**
  - BS-22: `_tick_bulldozer()` раньше только предупреждал и всё равно
    толкал снег, превышающий тягу — теперь честный abort в RETREAT.
  - BS-33: `GEOFENCE_MAX_ALTITUDE_M`/`GEOFENCE_ENABLED` были объявлены,
    но не применялись — добавлена проверка высоты в `_check_emergency()`.
  - BS-44: `TETHER_LENGTH_M` не валидировала досягаемость точки —
    добавлена проверка в `PerceptionModule.is_point_safe()` (рабочее
    допущение: станция в точке взлёта (0,0), задокументировано как
    ограничение, см. BS-48 в аудите).
  - BS-62: `MIN_ROOF_EDGE_DISTANCE_M` упоминалась в docstring, но не
    применялась — добавлены `utils.point_to_segment_distance()` и
    `utils.distance_to_polygon_boundary()`, используются в `is_point_safe()`.
  - BS-69: `SnowAssessment.confidence` вычислялся, но не использовался —
    теперь низкая средняя уверенность региона отменяет рекомендацию
    HOVER_BLOW в пользу более отказоустойчивого BULLDOZER.
  - BS-89: config.py не имел инвариантных проверок диапазонов — создан
    `tests/test_config.py` (30+ тестов: проценты в [0,100], упорядоченность
    порогов напряжения, монотонность TILT_THRUST_TABLE, и т.д.).
- `SafetyState.FAULT`/`BackupState.FAULT` (BS-77/78) сознательно НЕ
  тронуты — safety_monitor.py требует отдельного review по IEC 61010/60479
  согласно CLAUDE.md, риск непреднамеренного изменения safety-critical
  поведения выше уверенности <90% для правки вслепую (Золотое правило).

**Валидация:** Полная — ruff чист, mypy --strict чист, 446/446 тестов,
покрытие 100%.

**Нерешённые проблемы:** 93 находки из 99 остаются в бэклоге —
большинство требуют аппаратного (кожухи винтов, срезные штифты, IMD-датчик
и т.д.), архитектурного (asyncio вместо time.sleep, watchdog, персистентность
состояния) или процессного (ADR по showstopper-проблемам рецензии, полевой
чек-лист) решения, которое агент не может принять односторонне.

**Следующий шаг:** Дождаться приоритизации от оператора — какие из 93
оставшихся находок (особенно BS-99 — незакрытый вердикт «Безопасность
3/10») требуют немедленного внимания перед тем, как продолжать
наращивать функциональность поверх текущей архитектуры.

---

## [2026-07-31 11:05] Итерация 5

**Фаза:** Диагностика реальной ошибки `git push` (rejected) + мерж.

**Выполнено:**
- Настоящая причина «ошибок git»: между push'ами в этой сессии `origin`
  фактически сменился со внутреннего sandbox-прокси
  (`http://local_proxy@127.0.0.1:41729/...`) на боевой GitHub
  (`https://github.com/Leonidy431/VKTES`). Второй push (после добавления
  Hardware Design Protocol) был отклонён (`! [rejected] ... fetch first`),
  потому что оператор параллельно закоммитил на GitHub напрямую (веб-загрузка)
  два коммита: `da4458e` (новый файл `clauderc.md`, 457 строк — 99 правил
  инженерной дисциплины) и `a6a78cd` (добавление в конец `CLAUDE.md` 139
  строк — компактная версия тех же 99 лайфхаков).
- Проверено содержимое обоих коммитов перед действием (не вслепую): оба —
  чистые добавления (0 удалений), не пересекаются с правками этой сессии
  по расположению в файле. `git merge origin/... --no-edit` прошёл **без
  конфликтов** (auto-merge CLAUDE.md).
- Полный validation_protocol.md прогнан после мержа: ruff чист, mypy чист,
  395/395 тестов, покрытие 100%. Смёрженный `CLAUDE.md` вручную проверен
  по структуре заголовков — секции не перепутаны и не задвоены.
- Мерж запушен (`0a7b3c0`).
- `git ls-remote origin` на настоящем GitHub показывает: единственная
  ветка репозитория — `claude/write-boreas-readme-YYTjg`, и она же
  является `HEAD` (default branch) репозитория. Отдельного `main`/`master`
  не существует и не планировалось — эта ветка и есть трюк (trunk).
  Запрошенный пользователем «мерж» на этом закрыт: сливать больше некуда,
  всё уже в единственной боевой ветке.

**Валидация:** Полная, зелёная (см. выше).

**Нерешённые проблемы:** Обновить `BACKLOG.md` (снять пункт про
неопределённость целевой ветки мержа — он больше не актуален). KiCad/OpenSCAD
всё ещё ждут конкретного списка плат/разъёмов от оператора (без изменений).

**Следующий шаг:** Обновить BACKLOG.md, зафиксировав, что merge-target
вопрос закрыт (ветка сама является default branch).

---

## [2026-07-31 10:40] Итерация 4

**Фаза:** Регистрация нового стандартного протокола для аппаратного/OpenSCAD
дизайна.

**Выполнено:**
- В CLAUDE.md добавлен раздел «Hardware Design Protocol — OpenSCAD (DFM/DFA)»:
  персона Senior Hardware Engineer, обязательный цикл на каждую итерацию
  (прочность/печатность → сборочная совместимость → минимизация крепежа →
  сервисопригодность → строгая параметризация OpenSCAD с `clearance`/`eps`/
  `assembly()`), обязательный формат ответа (критика → архитектура → код).
  Зафиксировано, что новые `.scad`-файлы должны жить в `hardware/openscad/`
  с общим `params.scad`.
- Протокол помечен как «в режиме ожидания» — явно сослались на BACKLOG B-5
  (нет финального списка плат/разъёмов для KiCad/корпуса), поэтому первый
  параметрический узел ещё не запрошен пользователем с конкретными размерами.
- `pytest -q` перепроверен (395 passed) — правки только в CLAUDE.md, код не
  затронут.

**Валидация:** Полная для затронутой области (Markdown-файл читается корректно,
раздел синтаксически на месте между «Algorithm Selection» и «Useful skills»).
Тесты/lint/mypy не требовались для чисто документационного изменения, но
перепрогнаны для уверенности — зелёные.

**Нерешённые проблемы:** Тот же блокер, что и раньше (BACKLOG B-5) — ждём от
оператора конкретный список плат/разъёмов, чтобы дать первый ввод для этого
протокола (KiCad footprints и/или OpenSCAD корпус).

**Следующий шаг:** По готовности пользователя — принять первый ввод по
конкретному узлу (например, "корпус под Cube Orange+, разъём X") и запустить
цикл критика → архитектура → OpenSCAD-код.

---

## [2026-07-31 10:05] Итерация 3

**Фаза:** Исследование причины «ошибок git» + новые операционные правила.

**Выполнено:**
- Полный прогон validation_protocol.md: `ruff check` чист, `mypy --strict` чист,
  395/395 тестов, покрытие 100%. Working tree чист, локальная ветка `ff010fe`
  идентична `origin/claude/write-boreas-readme-YYTjg`.
- Найдена и задокументирована реальная причина более ранних «ошибок git» в этой
  сессии: в свежем контейнере локальный клон стартовал с настроенным remote, но
  без единого закоммиченного файла (`git status` → "No commits yet"), что
  выглядело как повреждение репозитория. Причина — контейнер пересоздался, но
  fetch/checkout ветки ещё не были выполнены. Восстановлено через
  `git fetch origin <branch> && git checkout -B <branch> origin/<branch>`.
- Проверено: `git ls-remote origin` показывает, что на удалённом репозитории
  существует **только** ветка `claude/write-boreas-readme-YYTjg` — нет
  `main`/`master`. `gh` CLI не установлен в этой среде. Значит, запрошенный
  пользователем «мерж» не может быть выполнен автоматически без уточнения:
  либо оператор указывает/создаёт целевую ветку, либо мерж делается вне этой
  сессии.
- Добавлены в CLAUDE.md два новых операционных правила (раздел
  «Development workflow»): (1) лимит 2 git push в день — работать пачками,
  прогонять validation_protocol.md один раз, потом пушить; (2) никогда не
  пушить/мержить при красном шаге протокола валидации.

**Валидация:** Полная — все шаги validation_protocol.md пройдены перед этой
записью (это единственное изменение в этой итерации — правки CLAUDE.md/
state_journal.md, не затрагивающие blueos/, но lint/tests всё равно
перепроверены для уверенности).

**Нерешённые проблемы:** См. `BACKLOG.md`. Дополнительно: запрос пользователя
на «мерж» блокирован отсутствием целевой ветки на remote — см. выше.

**Следующий шаг:** Дождаться от оператора уточнения целевой ветки для мержа
(или подтверждения, что нужно создать `main` с нуля из текущей ветки).

---

## [2026-07-31 09:10] Итерация 2

**Фаза:** Довести покрытие до 99%+ (продолжение итерации 1) + автономный
протокол + PEP8/типизация + документация/бэклог.

**Выполнено:**
- Покрытие `blueos/` доведено до **100%** (было 96% на конец итерации 1) —
  добиты пробелы в `backup_power.py`, `safety_monitor.py` (самый крупный
  пробел — 34 непокрытые строки), `tilt_controller.py`, `state_machine.py`,
  `thermal_analyzer.py`. Итого 395 тестов, все зелёные.
- Создан автономный протокол работы: `state_journal.md` (этот файл),
  `validation_protocol.md`, `.claudeignore`, `context_map.json`,
  `docs/ai_autonomy_lifehacks.md` (98 практик + золотое правило Rule 99),
  раздел «Autonomous Operation Protocol» в `CLAUDE.md`.
- `ruff check blueos/ tests/` → 0 замечаний (было 22; 19 авто-исправлено,
  3 вручную — два из них оказались реальными находками: `_burst_mode` в
  `main.py:414` вычисляется, но не применяется — см. `BACKLOG.md` B-1).
- `mypy blueos/ --strict --ignore-missing-imports` → 0 ошибок (было 7:
  отсутствующие type-параметры `list`/`dict`, неаннотированные параметры,
  некорректный тип у `max(key=type_area.get)`).
- CI-порог `--cov-fail-under` поднят с 70 до 99 в `.github/workflows/ci.yml`.
- Создан `BACKLOG.md` (найденные, но не исправленные в этой итерации вопросы:
  B-1 burst_mode wiring, B-2 RoofBoundary дублирует utils.py, B-3 docstring-
  аудит, B-4 Docker-заглушка, B-5 KiCad — ждёт уточнения scope).
- Создан `docs/API_REFERENCE.md` — публичный API всех 14 модулей `blueos/`.

**Валидация:** Полная — все 5 пунктов `validation_protocol.md` для кода
пройдены (git status чист, ruff чист, mypy --strict чист, 395/395 тестов
зелёные, покрытие 100% ≥ порога 99%).

**Нерешённые проблемы:** См. `BACKLOG.md` раздел «In Progress / Todo»
(B-1..B-5). Ни одна не блокирует текущую работу.

**Следующий шаг:** Дождаться уточнения от оператора по scope KiCad-задачи
(корпус + разъёмы под "стандартные выбранные платы") — см. BACKLOG B-5 —
прежде чем начинать генерацию KiCad-файлов вслепую.

---

## [2026-07-31 03:20] Итерация 1

**Фаза:** Повышение покрытия тестами `blueos/` с ~31% до 99%+.

**Выполнено:**
- Написаны тесты для 9 ранее непокрытых модулей: `utils.py`, `burst_vibrator.py`,
  `power_manager.py`, `mavlink_interface.py`, `perception.py`, `planner.py`,
  `hover_blow.py`, `thermal_analyzer.py`, `main.py` (`BoreasController`).
- Найден и исправлен реальный баг: `SURVEY → RETREAT` отсутствовал в
  `TRANSITIONS` конечного автомата (`blueos/state_machine.py`), из-за чего
  `_tick_survey()` при неудачной генерации плана зависал в состоянии SURVEY
  навсегда. Исправлено добавлением `State.RETREAT` в допустимые переходы из
  `State.SURVEY`.
- Помечен как `# pragma: no cover` мёртвый код в `thermal_analyzer.py`
  (ветка SETTLED в `_classify_region()` недостижима, пока
  `THERMAL_SNOW_TEMP_FRESH_C[1] == THERMAL_SNOW_TEMP_WET_C[0]` в config.py).
- 357 тестов проходят (`pytest tests/ -q`).

**Валидация:** Частично — `pytest -q` зелёный (357 passed). Финальный
`--cov=blueos --cov-report=term-missing` прогон и добор оставшихся пробелов
(`backup_power.py`, `safety_monitor.py`, `tilt_controller.py`) ещё не завершены.

**Нерешённые проблемы:**
- Итоговый процент покрытия ещё не измерен после последнего раунда правок.
- `ruff check` / `mypy --strict` не прогонялись в рамках этой итерации.

**Следующий шаг:** Прогнать `pytest --cov=blueos --cov-report=term-missing`,
добить недостающие ветки в `backup_power.py`/`safety_monitor.py`/
`tilt_controller.py`/`state_machine.py`, зафиксировать итоговый % в этом файле.
